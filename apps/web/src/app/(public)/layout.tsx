import { VendorReviewLayout } from "@/components/vendor";

export default function PublicLayout({ children }: { children: React.ReactNode }) {
  return <VendorReviewLayout>{children}</VendorReviewLayout>;
}
